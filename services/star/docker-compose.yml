version: '2.4'

services:
  star:
    build: .
    restart: unless-stopped
    ports:
      - "5555:5555"
    volumes:
      - "./data:/app/data"
#    environment:
#      GOMAXPROCS: 2
    pids_limit: 512
    mem_limit: 4G
    cpus: 2
